import 'package:flutter/material.dart';

class CompetitionsListScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Text('🏆 Competitions'));
  }
}
